# AoE2Kit API Reference

Generated by `cmd/apiref` from the kit binary itself. Invocation lines are the
binary's own usage output; output contracts were captured by executing read-only
commands against real fixtures. Commands that were not executed say so.

Regenerate with:

```sh
go build -o kit ./cmd/kit
go run ./cmd/apiref --kit ./kit \
  --scenario <file.aoe2scenario> --replay <file.aoe2record> \
  --dat <empires2_x2_p1.dat> --xsdat <file.xsdat>
```

## Calling conventions

- **JSON is the default output.** Add `--text` for a human-readable rendering.
  Agents should parse JSON and ignore `--text`.
- **Every report carries a verification label.** `structure_verified` means the
  parser/readback proved it. `engine_verified` means a live game or editor was the
  oracle. Labels beginning `behavior_inferred` or containing `candidate` are
  explicitly not proof. Never upgrade a claim past its label.
- **Exit codes.** `0` on success. Non-zero on parse failure, missing input, or a
  failed assertion. Assertion-bearing commands (`xsdat decode --ledger`,
  `verify-run`, `ci check`, `replay sidecar-sync --fail-on-mismatch`,
  `replay coverage --fail-on-opaque`) exit non-zero when the declared expectation
  is not met; that is the intended gate behavior, not a crash.
- **Writes go to a separate output file.** No command edits its input in place.
- **Resource guardrails.** Scenario inflation above 32 MiB is refused by default; raise
  with `AOE2KIT_MAX_SCENARIO_MB` or `AOE2KIT_ALLOW_HUGE_SCENARIO=1`. A 1 GiB soft
  heap limit applies unless `GOMEMLIMIT` or `AOE2KIT_GOMEMLIMIT_MB` is set.
  Large listings truncate at 200 rows unless `--limit 0`/`--all` is passed.
- **No external dependencies or network access**, except `replay fetch` and
  `player stats`, which call Microsoft's public endpoints.

## Probe status meanings

| status | meaning |
| --- | --- |
| `ok` | executed during generation; the listed output keys are real |
| `group_entry` | dispatch group, not a leaf command |
| `skipped_mutating` | writes files/registries; documented from usage only |
| `skipped_network` | performs network I/O; documented from usage only |
| `skipped_no_fixture` | no fixture of the required type was supplied |
| `requires_additional_input` | needs a caller-supplied contract/ledger/schema/recipe |
| `non_json_output` | emitted text rather than a JSON object |
| `probe_error` | execution failed; see the note |

Flags marked **required** in the tables below appear unbracketed in the tool's own
usage line and must be supplied.

Tool version `0.1.0` — 220 commands, 41 probed.

## Command index

- **(top level)** — `campaign`, `ci`, `collect`, `credits`, `dat`, `docs`, `doctor`, `facts`, `identify`, `inventory`, `manifest`, `mod`, `pack`, `portable-check`, `project`, `recipe`, `release-check`, `replay`, `roadmap`, `scen`, `summary`, `verify`, `verify-run`, `version`
- **ai** — `ai diff`, `ai fingerprint`, `ai lint`
- **campaign** — `campaign generate`
- **cba** — `cba archive`, `cba axes`, `cba balance`, `cba export`, `cba ingest`, `cba phase-facts`, `cba registry`, `cba replay`, `cba sidechannels`, `cba trigger-razes`, `cba trigger-spawns`
- **ci** — `ci check`, `ci init`
- **dat** — `dat abilities`, `dat ability`, `dat ability-create`, `dat ability-delete`, `dat ability-disable`, `dat ability-patch`, `dat availability`, `dat availability-set`, `dat civ-patch`, `dat civs`, `dat codec-patch`, `dat codec-plan`, `dat command-matrix`, `dat delete`, `dat delete-plan`, `dat diff`, `dat disconnect`, `dat effect`, `dat effect-command-delete`, `dat effect-create`, `dat effect-delete`, `dat effect-disable`, `dat effect-explain`, `dat effect-patch`, `dat effects`, `dat graphic`, `dat graphic-angle-sound-delete`, `dat graphic-create`, `dat graphic-delta-delete`, `dat graphic-patch`, `dat graphics`, `dat palette`, `dat patch`, `dat patch-graphic`, `dat patch-unit`, `dat plan`, `dat player-colour-create`, `dat player-colour-delete`, `dat player-colour-patch`, `dat player-colours`, `dat random-maps`, `dat refs`, `dat semantics-pack`, `dat semantics-readback`, `dat sound`, `dat sound-create`, `dat sound-delete`, `dat sound-item-delete`, `dat sound-patch`, `dat sounds`, `dat spans`, `dat sprite`, `dat tech`, `dat tech-create`, `dat tech-delete`, `dat tech-explain`, `dat tech-patch`, `dat tech-tree`, `dat tech-tree-connection-create`, `dat tech-tree-connection-delete`, `dat tech-tree-connection-patch`, `dat techs`, `dat terrain`, `dat terrain-patch`, `dat terrain-restriction-patch`, `dat terrain-restrictions`, `dat terrains`, `dat unit`, `dat unit-child-delete`, `dat unit-create`, `dat unit-delete`, `dat unit-header`, `dat unit-header-task-delete`, `dat unit-headers`, `dat units`
- **docs** — `docs lint`
- **fx** — `fx bind`, `fx lint`, `fx new`
- **gfx** — `gfx export`, `gfx info`
- **player** — `player stats`
- **project** — `project diff`, `project inspect`, `project lineage`, `project snapshot`
- **recipe** — `recipe export`, `recipe list`, `recipe show`
- **replay** — `replay actions`, `replay ai-manifest`, `replay carrier`, `replay chat`, `replay checksum-phase`, `replay checksum-probe`, `replay combat`, `replay corpus`, `replay coverage`, `replay datamod-check`, `replay deaths`, `replay diff-state`, `replay diff-triggers`, `replay effective-data`, `replay effective-units`, `replay events`, `replay feedback`, `replay fetch`, `replay frontier`, `replay header-anchors`, `replay inbox`, `replay info`, `replay issues`, `replay lifecycle`, `replay object-shapes`, `replay object-state`, `replay objects`, `replay opaque-clusters`, `replay opaque-spans`, `replay player-events`, `replay player-profile`, `replay player-series`, `replay playtest`, `replay postgame`, `replay scan-value`, `replay sidecar-sync`, `replay spawns`, `replay story`, `replay sync`, `replay sync-log`, `replay telemetry`, `replay trigger-neighborhood`, `replay triggers`, `replay unknowns`, `replay xs-telemetry`
- **roadmap** — `roadmap crud`, `roadmap dark`, `roadmap rwd`
- **rpg** — `rpg shop-demo`
- **scen** — `scen analyze`, `scen audit-player-coverage`, `scen blank`, `scen delete`, `scen delete-plan`, `scen deploycheck`, `scen describe`, `scen diff`, `scen diff-triggers`, `scen disconnect`, `scen effects`, `scen glossary`, `scen idioms`, `scen lint`, `scen mechanic`, `scen palette-usage`, `scen patch`, `scen plan`, `scen refs`, `scen settings`, `scen smoke`, `scen smoke-recipe`, `scen strings`, `scen terrain`, `scen trigger-flow`, `scen trigger-neighborhood`, `scen triggers`, `scen units`, `scen write-check`, `scen xs`, `scen xs attach`, `scen xs compare`, `scen xs deploy`, `scen xs embed`, `scen xs extract`
- **swatch** — `swatch patterns`
- **xs** — `xs bridge`, `xs datagen`, `xs inspect`, `xs shims`
- **xsdat** — `xsdat decode`

## (top level)

### `campaign`

```
kit campaign <generate> ...
```

- probe: `skipped_no_fixture`

### `ci`

```
kit ci <init|check> ...
```

Subcommands: `init`, `check`

- probe: `group_entry`

### `collect`

```
kit collect <folder> [--registry known_scenarios.json] [--dry-run]
```

| flag | value |
| --- | --- |
| `--registry` | `known_scenarios.json` |
| `--dry-run` | _(boolean)_ |

**writes output files**

- probe: `skipped_mutating`

### `credits`

```
kit credits [notice] [folder] [--text|--json]
```

- probe: `ok`
- JSON top-level keys: `dependencies`, `generated_by`, `license`, `project`, `references`, `schema`
- response type: `kit.CreditsDocument` (full nested shape in `api_schemas.json`)

### `dat`

```
kit dat <info|check|roundtrip|codec-plan|codec-patch|delete-plan|delete|plan|patch|...> <empires*.dat>
```

- probe: `non_json_output`
- note: usage: kit dat <info|check|roundtrip|diff|unit-headers|unit-header|civs|spans|graphics|graphic|effects|effect|techs|tech|abilities|ability|tech-tree|units|unit|availability|terrain-restrictions|terrai...

### `docs`

```
kit docs <lint> ...
```

- probe: `group_entry`

### `doctor`

```
kit doctor
```

- probe: `non_json_output`
- note: AoE2Kit doctor

### `facts`

```
kit facts <list|check> [--include-provisional] [--domain DOMAIN] [--text]
```

Subcommands: `list`, `check`

| flag | value |
| --- | --- |
| `--include-provisional` | _(boolean)_ |
| `--domain` | `DOMAIN` |
| `--text` | _(boolean)_ |

- probe: `group_entry`

### `identify`

```
kit identify <file.aoe2scenario|file.aoe2record> [--registry known_scenarios.json] [--ai-registry known_ais.json] [--register NAME] [--family FAMILY] [--description TEXT]
```

| flag | value |
| --- | --- |
| `--registry` | `known_scenarios.json` |
| `--ai-registry` | `known_ais.json` |
| `--register` | `NAME` |
| `--family` | `FAMILY` |
| `--description` | `TEXT` |

- probe: `skipped_no_fixture`

### `inventory`

```
kit inventory [folder]
```

- probe: `ok`
- JSON top-level keys: `files`, `mod_dirs`, `root`
- response type: `kit.Inventory` (full nested shape in `api_schemas.json`)

### `manifest`

```
kit manifest
```

- probe: `ok`
- JSON top-level keys: `commands`, `docs`, `domains`, `limitations`, `name`, `neutrality`, `runtime`, `version`
- response type: `kit.Manifest` (full nested shape in `api_schemas.json`)

### `mod`

```
kit mod <check|inspect> <moddir> [--status mod-status.json]
```

Subcommands: `check`, `inspect`

| flag | value |
| --- | --- |
| `--status` | `mod-status.json` |

- probe: `group_entry`

### `pack`

```
kit pack [out.zip] [folder] [--profile full|handoff|sandbox|public]
```

| flag | value |
| --- | --- |
| `--profile` | `full|handoff|sandbox|public` |

**writes output files**

- probe: `skipped_mutating`

### `portable-check`

```
kit portable-check [folder] [--profile full|handoff|sandbox|public]
```

| flag | value |
| --- | --- |
| `--profile` | `full|handoff|sandbox|public` |

- probe: `ok`
- JSON top-level keys: `checks`, `errors`, `ok`, `root`
- response type: `kit.PortableReport` (full nested shape in `api_schemas.json`)

### `project`

```
kit project <inspect|snapshot|diff|lineage> ...
```

Subcommands: `inspect`, `snapshot`, `diff`, `lineage`

- probe: `group_entry`

### `recipe`

```
kit recipe <list|show> ...
```

Subcommands: `list`, `show`

- probe: `non_json_output`
- note: usage: kit recipe <list|show> ...

### `release-check`

```
kit release-check <scenario.aoe2scenario> [--previous old.aoe2scenario] [--mod moddir] [--status mod-status.json] [--pack out.zip] [--text]
```

| flag | value |
| --- | --- |
| `--previous` | `old.aoe2scenario` |
| `--mod` | `moddir` |
| `--status` | `mod-status.json` |
| `--pack` | `out.zip` |
| `--text` | _(boolean)_ |

**writes output files**

- probe: `skipped_mutating`

### `replay`

```
kit replay <info|summary|graph|triggers|trigger-neighborhood|diff-triggers|coverage|opaque-spans|frontier|corpus|opaque-clusters|ai-manifest|effective-data|effective-units|datamod-check|fetch|diff-state|scan-value|header-anchors|objects|object-state|object-shapes|spawns|lifecycle|postgame|camera|sync|sync-log|checksum-phase|checksum-probe|combat|deaths|player-series|playtest|carrier|sidecar-sync|xs-telemetry|events|actions|player-events|chat|feedback|story|player-profile|inbox|telemetry|issues|unknowns> <path>
```

Subcommands: `info`, `summary`, `graph`, `triggers`, `trigger-neighborhood`, `diff-triggers`, `coverage`, `opaque-spans`, `frontier`, `corpus`, `opaque-clusters`, `ai-manifest`, `effective-data`, `effective-units`, `datamod-check`, `fetch`, `diff-state`, `scan-value`, `header-anchors`, `objects`, `object-state`, `object-shapes`, `spawns`, `lifecycle`, `postgame`, `camera`, `sync`, `sync-log`, `checksum-phase`, `checksum-probe`, `combat`, `deaths`, `player-series`, `playtest`, `carrier`, `sidecar-sync`, `xs-telemetry`, `events`, `actions`, `player-events`, `chat`, `feedback`, `story`, `player-profile`, `inbox`, `telemetry`, `issues`, `unknowns`

- probe: `group_entry`

### `roadmap`

```
kit roadmap <crud|rwd|dark> [--domain scenario|dat|replay|all] [--text|--json]
```

Subcommands: `crud`, `rwd`, `dark`

| flag | value |
| --- | --- |
| `--domain` | `scenario|dat|replay|all` |

- probe: `non_json_output`
- note: usage: kit roadmap <rwd|dark> [--domain scenario|dat|replay|all] [--text|--json]

### `scen`

```
kit scen <blank|info|check|describe|settings|strings|refs|delete-plan|delete|disconnect|effects|glossary|idioms|analyze|triggers|units|map|terrain|palette-usage|regions|verify|lint|xs|deploycheck|diff|diff-triggers|write-check|plan|patch|smoke|smoke-recipe> <file.aoe2scenario>
```

Subcommands: `blank`, `info`, `check`, `describe`, `settings`, `strings`, `refs`, `delete-plan`, `delete`, `disconnect`, `effects`, `glossary`, `idioms`, `analyze`, `triggers`, `units`, `map`, `terrain`, `palette-usage`, `regions`, `verify`, `lint`, `xs`, `deploycheck`, `diff`, `diff-triggers`, `write-check`, `plan`, `patch`, `smoke`, `smoke-recipe`

- probe: `group_entry`

### `summary`

```
kit summary [folder]
```

- probe: `non_json_output`
- note: root: .

### `verify`

```
kit verify [folder] [--go-checks]
```

| flag | value |
| --- | --- |
| `--go-checks` | _(boolean)_ |

- probe: `ok`
- verification: `structure_failed`
- JSON top-level keys: `artifacts_ok`, `docs_ok`, `errors`, `go_available`, `inventory_ok`, `manifest_ok`, `root`, `source_build_ok`, `verification`, `version`
- response type: `kit.VerifyReport` (full nested shape in `api_schemas.json`)

### `verify-run`

```
kit verify-run <replay.aoe2record|replay.zip> --contract contract.json [--text]
```

| flag | value |
| --- | --- |
| `--contract` | `contract.json` **(required)** |
| `--text` | _(boolean)_ |

- probe: `requires_additional_input`
- note: required --contract must be supplied by the caller

### `version`

```
kit version
```

- probe: `non_json_output`
- note: 0.1.0

## ai

### `ai diff`

```
kit ai diff <before-file-or-folder> <after-file-or-folder> [--text]
```

| flag | value |
| --- | --- |
| `--text` | _(boolean)_ |

- probe: `skipped_no_fixture`

### `ai fingerprint`

```
kit ai fingerprint <file-or-folder> [--registry known_ais.json] [--register NAME] [--signature AI_FILES_SIGNATURE]
```

| flag | value |
| --- | --- |
| `--registry` | `known_ais.json` |
| `--register` | `NAME` |
| `--signature` | `AI_FILES_SIGNATURE` |

**writes output files**

- probe: `skipped_mutating`

### `ai lint`

```
kit ai lint <file-or-folder> [--text]
```

| flag | value |
| --- | --- |
| `--text` | _(boolean)_ |

- probe: `skipped_no_fixture`

## campaign

### `campaign generate`

```
kit campaign generate <campaign.json> [--out-dir DIR] [--prefix A2KCampaign] [--text]
```

| flag | value |
| --- | --- |
| `--out-dir` | `DIR` |
| `--prefix` | `A2KCampaign` |
| `--text` | _(boolean)_ |

**writes output files**

- probe: `skipped_mutating`

## cba

### `cba archive`

```
kit cba archive --queue <q.json> --out <dir> --max N [--delay-ms MS] [--manifest f.jsonl]
```

| flag | value |
| --- | --- |
| `--queue` | _(boolean)_ **(required)** |
| `--out` | _(boolean)_ **(required)** |
| `--max` | `N` **(required)** |
| `--delay-ms` | `MS` |
| `--manifest` | `f.jsonl` |

**network I/O**

- probe: `skipped_network`

### `cba axes`

```
kit cba axes --corpus <file.jsonl> [--min-games N] [--names <file.json>] [--text]
```

| flag | value |
| --- | --- |
| `--corpus` | _(boolean)_ **(required)** |
| `--min-games` | `N` |
| `--names` | _(boolean)_ |
| `--text` | _(boolean)_ |

- probe: `requires_additional_input`
- note: required --corpus must be supplied by the caller

### `cba balance`

```
kit cba balance [--text|--json]
```

- probe: `ok`
- method: `source_labeled_cba_requiem_balance_table`
- verification: `partial_changelog_rows_plus_trigger_graph_global_threshold_evidence`
- JSON top-level keys: `evidence`, `method`, `missing`, `rows`, `summary`, `verification`, `warnings`
- response type: `cba.BalanceReport` (full nested shape in `api_schemas.json`)

### `cba export`

```
kit cba export --corpus <file.jsonl> [--names <file.json>] [--sql]
```

| flag | value |
| --- | --- |
| `--corpus` | _(boolean)_ **(required)** |
| `--names` | _(boolean)_ |
| `--sql` | _(boolean)_ |

- probe: `requires_additional_input`
- note: required --corpus must be supplied by the caller

### `cba ingest`

```
kit cba ingest --corpus <file.jsonl> (--registry <file.json>|--unverified <replay|dir>...) [--force] [--limit N]
```

| flag | value |
| --- | --- |
| `--corpus` | _(boolean)_ **(required)** |
| `--force` | _(boolean)_ |
| `--limit` | `N` |

**writes output files**

- probe: `skipped_mutating`

### `cba phase-facts`

```
kit cba phase-facts <file.aoe2record|replay.zip> --dat <empires2_x2_p1.dat> [--text]
```

| flag | value |
| --- | --- |
| `--dat` | _(boolean)_ **(required)** |
| `--text` | _(boolean)_ |

- probe: `skipped_no_fixture`

### `cba registry`

```
kit cba registry <replay|dir>... [--out <file.json>] [--text]
```

| flag | value |
| --- | --- |
| `--out` | _(boolean)_ |
| `--text` | _(boolean)_ |

**writes output files**

- probe: `skipped_mutating`

### `cba replay`

```
kit cba replay <progression|razes|raze-pressure|phases|perf|doctrine> <file.aoe2record|replay.zip> [--dat <empires2_x2_p1.dat>] [--text|--json]
```

Subcommands: `progression`, `razes`, `raze-pressure`, `phases`, `perf`, `doctrine`

| flag | value |
| --- | --- |
| `--dat` | _(boolean)_ |

- probe: `group_entry`

### `cba sidechannels`

```
kit cba sidechannels <file.aoe2record|replay.zip> [--text]
```

| flag | value |
| --- | --- |
| `--text` | _(boolean)_ |

- probe: `skipped_no_fixture`

### `cba trigger-razes`

```
kit cba trigger-razes <file.aoe2record|replay.zip> [--dat <empires2_x2_p1.dat>] [--text]
```

| flag | value |
| --- | --- |
| `--dat` | _(boolean)_ |
| `--text` | _(boolean)_ |

- probe: `skipped_no_fixture`

### `cba trigger-spawns`

```
kit cba trigger-spawns <file.aoe2record|replay.zip> [--dat <empires2_x2_p1.dat>] [--text]
```

| flag | value |
| --- | --- |
| `--dat` | _(boolean)_ |
| `--text` | _(boolean)_ |

- probe: `skipped_no_fixture`

## ci

### `ci check`

```
kit ci check [aoe2kit-ci.json] [--text]
```

| flag | value |
| --- | --- |
| `--text` | _(boolean)_ |

- probe: `skipped_no_fixture`

### `ci init`

```
kit ci init [scenario.aoe2scenario] [--root DIR] [--overwrite] [--text]
```

| flag | value |
| --- | --- |
| `--root` | `DIR` |
| `--overwrite` | _(boolean)_ |
| `--text` | _(boolean)_ |

**writes output files**

- probe: `skipped_mutating`

## dat

### `dat abilities`

```
kit dat abilities <empires*.dat> [--limit N|--all] [--name-contains TEXT] [--effect-id N] [--command-type N] [--operand N]
```

| flag | value |
| --- | --- |
| `--limit` | `N|--all` |
| `--name-contains` | `TEXT` |
| `--effect-id` | `N` |
| `--command-type` | `N` |
| `--operand` | `N` |

- probe: `ok`
- verification: `structure_verified_not_engine_verified`
- JSON top-level keys: `abilities`, `effect_count`, `filtered_count`, `filters`, `returned_count`, `tech_count`, `truncated`, `verification`, `version`

### `dat ability`

```
kit dat ability <empires*.dat> <tech_id>
```

- probe: `non_json_output`
- note: usage: kit dat ability <empires*.dat> <tech_id>

### `dat ability-create`

```
kit dat ability-create <in.dat> <out.dat> --from-tech N --name TEXT [--effect-name TEXT] [--from-effect N] [--command type,a,b,c,d|json] [--append-command type,a,b,c,d|json] [tech flags]
```

| flag | value |
| --- | --- |
| `--from-tech` | `N` **(required)** |
| `--name` | `TEXT` **(required)** |
| `--effect-name` | `TEXT` |
| `--from-effect` | `N` |
| `--command` | `type,a,b,c,d|json` |
| `--append-command` | `type,a,b,c,d|json` |

**writes output files**

- probe: `skipped_mutating`

### `dat ability-delete`

```
kit dat ability-delete <in.dat> <out.dat> <tech_id>
```

**writes output files**

- probe: `skipped_mutating`

### `dat ability-disable`

```
kit dat ability-disable <in.dat> <out.dat> <tech_id>
```

**writes output files**

- probe: `skipped_mutating`

### `dat ability-patch`

```
kit dat ability-patch <in.dat> <out.dat> <tech_id> [--name TEXT] [--effect-name TEXT] [--clear-commands] [--command type,a,b,c,d|json] [--append-command type,a,b,c,d|json] [--remove-command N] [tech flags]
```

| flag | value |
| --- | --- |
| `--name` | `TEXT` |
| `--effect-name` | `TEXT` |
| `--clear-commands` | _(boolean)_ |
| `--command` | `type,a,b,c,d|json` |
| `--append-command` | `type,a,b,c,d|json` |
| `--remove-command` | `N` |

**writes output files**

- probe: `skipped_mutating`

### `dat availability`

```
kit dat availability <empires*.dat> <unit_id> [--civ N|--all-civs]
```

| flag | value |
| --- | --- |
| `--civ` | `N|--all-civs` |

- probe: `ok`
- verification: `structure_verified_not_engine_verified`
- JSON top-level keys: `cards`, `request`, `summary`, `verification`, `version`
- response type: `datcodec.UnitAvailabilityCardsReport` (full nested shape in `api_schemas.json`)

### `dat availability-set`

```
kit dat availability-set <in.dat> <out.dat> <unit_id> (--civ N|--all-civs) --enabled true|false|1|0
```

| flag | value |
| --- | --- |
| `--enabled` | `true|false|1|0` **(required)** |

**writes output files**

- probe: `skipped_mutating`

### `dat civ-patch`

```
kit dat civ-patch <in.dat> <out.dat> <civ_id> [--name TEXT] [--tech-tree-id N] [--team-bonus-id N] [--icon-set N] [--resource INDEX,VALUE]
```

| flag | value |
| --- | --- |
| `--name` | `TEXT` |
| `--tech-tree-id` | `N` |
| `--team-bonus-id` | `N` |
| `--icon-set` | `N` |
| `--resource` | `INDEX,VALUE` |

**writes output files**

- probe: `skipped_mutating`

### `dat civs`

```
kit dat civs <empires*.dat>
```

- probe: `ok`
- JSON top-level keys: `civs`, `version`

### `dat codec-patch`

```
kit dat codec-patch <in.dat> <out.dat> --recipe recipe.json
```

| flag | value |
| --- | --- |
| `--recipe` | `recipe.json` **(required)** |

**writes output files**

- probe: `skipped_mutating`

### `dat codec-plan`

```
kit dat codec-plan <in.dat> --recipe recipe.json
```

| flag | value |
| --- | --- |
| `--recipe` | `recipe.json` **(required)** |

**writes output files**

- probe: `skipped_mutating`

### `dat command-matrix`

```
kit dat command-matrix <empires*.dat> [--limit examples_per_type|--all] [--command-type N] [--reference-kind unit|tech] [--reference-field FIELD] [--typed-only|--unknown-only|--candidate-only] [--text|--json]
```

| flag | value |
| --- | --- |
| `--limit` | `examples_per_type|--all` |
| `--command-type` | `N` |
| `--reference-kind` | `unit|tech` |
| `--reference-field` | `FIELD` |

- probe: `skipped_no_fixture`

### `dat delete`

```
kit dat delete <in.dat> <out.dat> <section|effect-command|sound-item|graphic-row-kind|unit-header-task|unit-row-kind> <target> [--civ N|--all-civs]
```

| flag | value |
| --- | --- |
| `--civ` | `N|--all-civs` |

**writes output files**

- probe: `skipped_no_fixture`
- verification: `structure_verified_not_engine_verified`
- JSON top-level keys: `delete_plan`, `patch_report`, `request`, `verification`, `version`

### `dat delete-plan`

```
kit dat delete-plan <in.dat> <section|effect-command|sound-item|graphic-row-kind|unit-header-task|unit-row-kind> <target> [--civ N|--all-civs] [--text]
```

| flag | value |
| --- | --- |
| `--civ` | `N|--all-civs` |
| `--text` | _(boolean)_ |

- probe: `ok`
- verification: `structure_verified_not_engine_verified`
- JSON top-level keys: `mutates`, `recipe_hint`, `request`, `strategy`, `supported`, `verification`, `version`, `warnings`
- response type: `datcodec.DeletePlanReport` (full nested shape in `api_schemas.json`)

### `dat diff`

```
kit dat diff <base.dat> <mod.dat> [--section NAME|all] [--limit N|--all] [--text|--json]
```

| flag | value |
| --- | --- |
| `--section` | `NAME|all` |
| `--limit` | `N|--all` |

- probe: `non_json_output`
- note: usage: kit dat diff <base.dat> <mod.dat> [--section NAME|all] [--limit N|--all] [--text]

### `dat disconnect`

```
kit dat disconnect <in.dat> <out.dat> <tech|unit> <id>
```

**writes output files**

- probe: `skipped_no_fixture`
- verification: `structure_verified_not_engine_verified`
- JSON top-level keys: `patch_report`, `reference_summary`, `request`, `verification`, `version`

### `dat effect`

```
kit dat effect <empires*.dat> <effect_id>
```

- probe: `ok`
- JSON top-level keys: `command_size`, `commands`, `index`, `name`, `span`
- response type: `datfile.Effect` (full nested shape in `api_schemas.json`)

### `dat effect-command-delete`

```
kit dat effect-command-delete <in.dat> <out.dat> <effect_id> <command_index>
```

**writes output files**

- probe: `skipped_mutating`

### `dat effect-create`

```
kit dat effect-create <in.dat> <out.dat> --name TEXT [--from-effect N] [--command type,a,b,c,d|json] [--append-command type,a,b,c,d|json] [--remove-command N]
```

| flag | value |
| --- | --- |
| `--name` | `TEXT` **(required)** |
| `--from-effect` | `N` |
| `--command` | `type,a,b,c,d|json` |
| `--append-command` | `type,a,b,c,d|json` |
| `--remove-command` | `N` |

**writes output files**

- probe: `skipped_mutating`

### `dat effect-delete`

```
kit dat effect-delete <in.dat> <out.dat> <effect_id>
```

**writes output files**

- probe: `skipped_mutating`

### `dat effect-disable`

```
kit dat effect-disable <in.dat> <out.dat> <effect_id>
```

**writes output files**

- probe: `skipped_mutating`

### `dat effect-explain`

```
kit dat effect-explain <empires*.dat> <effect_id> [--text|--json]
```

- probe: `skipped_no_fixture`

### `dat effect-patch`

```
kit dat effect-patch <in.dat> <out.dat> <effect_id> [--name TEXT] [--clear-commands] [--command type,a,b,c,d|json] [--append-command type,a,b,c,d|json] [--remove-command N]
```

| flag | value |
| --- | --- |
| `--name` | `TEXT` |
| `--clear-commands` | _(boolean)_ |
| `--command` | `type,a,b,c,d|json` |
| `--append-command` | `type,a,b,c,d|json` |
| `--remove-command` | `N` |

**writes output files**

- probe: `skipped_mutating`

### `dat effects`

```
kit dat effects <empires*.dat> [--limit N|--all] [--command-type N] [--operand N]
```

| flag | value |
| --- | --- |
| `--limit` | `N|--all` |
| `--command-type` | `N` |
| `--operand` | `N` |

- probe: `ok`
- JSON top-level keys: `effect_count`, `effects`, `filtered_count`, `returned_count`, `truncated`, `version`

### `dat graphic`

```
kit dat graphic <empires*.dat> <graphic_id> [--spans|--raw]
```

- probe: `ok`
- JSON top-level keys: `angle_count`, `angle_sound_count`, `delta_count`, `file_name`, `frame_count`, `frame_duration`, `id`, `index`, `layer`, `name`, `particle_effect_name`, `player_color`, `record_end`, `record_start`, `replay_delay`, `sequence_type`, `slp`, `sound_id`, `speed_multiplier`, `wwise_sound_id`
- response type: `datfile.GraphicSummary` (full nested shape in `api_schemas.json`)

### `dat graphic-angle-sound-delete`

```
kit dat graphic-angle-sound-delete <in.dat> <out.dat> <graphic_id> <row_index>
```

**writes output files**

- probe: `skipped_mutating`

### `dat graphic-create`

```
kit dat graphic-create <in.dat> <out.dat> --from N [graphic scalar flags]
```

| flag | value |
| --- | --- |
| `--from` | `N` **(required)** |

**writes output files**

- probe: `skipped_mutating`

### `dat graphic-delta-delete`

```
kit dat graphic-delta-delete <in.dat> <out.dat> <graphic_id> <row_index>
```

**writes output files**

- probe: `skipped_mutating`

### `dat graphic-patch`

```
kit dat graphic-patch <in.dat> <out.dat> <graphic_id> [graphic scalar flags]
```

**writes output files**

- probe: `skipped_mutating`

### `dat graphics`

```
kit dat graphics <empires*.dat> [--limit N|--all] [--particle] [--name-contains TEXT] [--spans|--raw]
```

| flag | value |
| --- | --- |
| `--limit` | `N|--all` |
| `--particle` | _(boolean)_ |
| `--name-contains` | `TEXT` |

- probe: `ok`
- JSON top-level keys: `graphics_size`, `inflated_bytes`, `matching_count`, `present_count`, `present_graphics`, `returned_count`, `truncated`, `version`

### `dat palette`

```
kit dat palette <empires*.dat> [--id N] [--min-variants N] [--json|--text]
```

| flag | value |
| --- | --- |
| `--id` | `N` |
| `--min-variants` | `N` |

- probe: `skipped_no_fixture`

### `dat patch`

```
kit dat patch <in.dat> <out.dat> --recipe recipe.json
```

| flag | value |
| --- | --- |
| `--recipe` | `recipe.json` **(required)** |

**writes output files**

- probe: `skipped_mutating`

### `dat patch-graphic`

```
kit dat patch-graphic <in.dat> <out.dat> <graphic_id> [graphic scalar flags]
```

**writes output files**

- probe: `skipped_mutating`

### `dat patch-unit`

```
kit dat patch-unit <in.dat> <out.dat> <civ_id> <unit_id> [scalar flags] [--type50-attack I,field=value] [--cost I,field=value] [--task I,field=value]
```

| flag | value |
| --- | --- |
| `--type50-attack` | `I,field=value` |
| `--cost` | `I,field=value` |
| `--task` | `I,field=value` |

**writes output files**

- probe: `skipped_mutating`

### `dat plan`

```
kit dat plan <in.dat> --recipe recipe.json
```

| flag | value |
| --- | --- |
| `--recipe` | `recipe.json` **(required)** |

**writes output files**

- probe: `skipped_mutating`

### `dat player-colour-create`

```
kit dat player-colour-create <in.dat> <out.dat> --from N [colour-flags]
```

| flag | value |
| --- | --- |
| `--from` | `N` **(required)** |

**writes output files**

- probe: `skipped_mutating`

### `dat player-colour-delete`

```
kit dat player-colour-delete <in.dat> <out.dat> <colour_id>
```

**writes output files**

- probe: `skipped_mutating`

### `dat player-colour-patch`

```
kit dat player-colour-patch <in.dat> <out.dat> <colour_id> [colour-flags]
```

**writes output files**

- probe: `skipped_mutating`

### `dat player-colours`

```
kit dat player-colours <empires*.dat>
```

- probe: `ok`
- JSON top-level keys: `colour_count`, `player_colours`, `version`

### `dat random-maps`

```
kit dat random-maps <empires*.dat>
```

- probe: `ok`
- JSON top-level keys: `map_count`, `note`, `random_maps`, `total_elevations`, `total_lands`, `total_terrains`, `total_units`, `version`

### `dat refs`

```
kit dat refs <in.dat> <section> <id> [--class CLASS] [--confidence NAME] [--source-section SECTION] [--limit N] [--text|--json]
```

| flag | value |
| --- | --- |
| `--class` | `CLASS` |
| `--confidence` | `NAME` |
| `--source-section` | `SECTION` |
| `--limit` | `N` |

- probe: `ok`
- verification: `structure_verified_not_engine_verified`
- JSON top-level keys: `references`, `request`, `summary`, `verification`, `version`
- response type: `datcodec.ReferenceReport` (full nested shape in `api_schemas.json`)

### `dat semantics-pack`

```
kit dat semantics-pack <in.dat> <out-dir> [--feature full|local-building-effects]
```

| flag | value |
| --- | --- |
| `--feature` | `full|local-building-effects` |

**writes output files**

- probe: `skipped_mutating`

### `dat semantics-readback`

```
kit dat semantics-readback <expected.json> --dat empires*.dat --scenario file.aoe2scenario --replay file.aoe2record [--out report.md] [--text]
```

| flag | value |
| --- | --- |
| `--dat` | `empires*.dat` **(required)** |
| `--scenario` | `file.aoe2scenario` **(required)** |
| `--replay` | `file.aoe2record` **(required)** |
| `--out` | `report.md` |
| `--text` | _(boolean)_ |

**writes output files**

- probe: `skipped_mutating`

### `dat sound`

```
kit dat sound <empires*.dat> <sound_id>
```

- probe: `ok`
- JSON top-level keys: `cache_time`, `id`, `index`, `items`, `play_delay`, `span`, `total_probability`
- response type: `datfile.Sound` (full nested shape in `api_schemas.json`)

### `dat sound-create`

```
kit dat sound-create <in.dat> <out.dat> --from N [sound-flags] [--item file,resource,probability,civ,icon_set]
```

| flag | value |
| --- | --- |
| `--from` | `N` **(required)** |
| `--item` | `file,resource,probability,civ,icon_set` |

**writes output files**

- probe: `skipped_mutating`

### `dat sound-delete`

```
kit dat sound-delete <in.dat> <out.dat> <sound_id>
```

**writes output files**

- probe: `skipped_mutating`

### `dat sound-item-delete`

```
kit dat sound-item-delete <in.dat> <out.dat> <sound_id> <item_index>
```

**writes output files**

- probe: `skipped_mutating`

### `dat sound-patch`

```
kit dat sound-patch <in.dat> <out.dat> <sound_id> [sound-flags] [--item INDEX,field=value...] [--remove-item N]
```

| flag | value |
| --- | --- |
| `--item` | `INDEX,field=value...` |
| `--remove-item` | `N` |

**writes output files**

- probe: `skipped_mutating`

### `dat sounds`

```
kit dat sounds <empires*.dat> [--limit N|--all]
```

| flag | value |
| --- | --- |
| `--limit` | `N|--all` |

- probe: `ok`
- JSON top-level keys: `item_count`, `returned_count`, `sound_count`, `sounds`, `truncated`, `version`

### `dat spans`

```
kit dat spans <empires*.dat>
```

- probe: `ok`
- JSON top-level keys: `inflated_bytes`, `span_count`, `spans`, `version`

### `dat sprite`

```
kit dat sprite <file.sld> [--out dir] [--limit N] [--text]
```

| flag | value |
| --- | --- |
| `--out` | `dir` |
| `--limit` | `N` |
| `--text` | _(boolean)_ |

**writes output files**

- probe: `skipped_mutating`

### `dat tech`

```
kit dat tech <empires*.dat> <tech_id>
```

- probe: `ok`
- JSON top-level keys: `civ`, `effect_id`, `full_tech_mode`, `icon_id`, `index`, `language_dll_description`, `language_dll_help`, `language_dll_name`, `language_dll_tech_tree`, `name`, `repeatable`, `required_tech_count`, `required_techs`, `research_locations`, `resource_costs`, `span`, `type`
- response type: `datfile.Tech` (full nested shape in `api_schemas.json`)

### `dat tech-create`

```
kit dat tech-create <in.dat> <out.dat> --from N [tech-flags]
```

| flag | value |
| --- | --- |
| `--from` | `N` **(required)** |

**writes output files**

- probe: `skipped_mutating`

### `dat tech-delete`

```
kit dat tech-delete <in.dat> <out.dat> <tech_id>
```

**writes output files**

- probe: `skipped_mutating`

### `dat tech-explain`

```
kit dat tech-explain <empires*.dat> <tech_id> [--text|--json]
```

- probe: `skipped_no_fixture`

### `dat tech-patch`

```
kit dat tech-patch <in.dat> <out.dat> <tech_id> [tech-flags]
```

**writes output files**

- probe: `skipped_mutating`

### `dat tech-tree`

```
kit dat tech-tree <empires*.dat> [--full]
```

| flag | value |
| --- | --- |
| `--full` | _(boolean)_ |

- probe: `ok`
- JSON top-level keys: `age_count`, `building_count`, `game_metrics`, `research_count`, `span`, `total_unit_tech_groups`, `unit_count`, `version`

### `dat tech-tree-connection-create`

```
kit dat tech-tree-connection-create <in.dat> <out.dat> <building|unit|research> <from_index> [connection flags]
```

**writes output files**

- probe: `skipped_mutating`

### `dat tech-tree-connection-delete`

```
kit dat tech-tree-connection-delete <in.dat> <out.dat> <building|unit|research> <index>
```

**writes output files**

- probe: `skipped_mutating`

### `dat tech-tree-connection-patch`

```
kit dat tech-tree-connection-patch <in.dat> <out.dat> <building|unit|research> <index> [connection flags]
```

**writes output files**

- probe: `skipped_mutating`

### `dat techs`

```
kit dat techs <empires*.dat> [--limit N|--all] [--name-contains TEXT]
```

| flag | value |
| --- | --- |
| `--limit` | `N|--all` |
| `--name-contains` | `TEXT` |

- probe: `ok`
- JSON top-level keys: `filtered_count`, `filters`, `returned_count`, `tech_count`, `techs`, `truncated`, `version`

### `dat terrain`

```
kit dat terrain <empires*.dat> <terrain_id>
```

- probe: `ok`
- JSON top-level keys: `index`, `name`, `name_2`, `overlay_mask_name`, `span`
- response type: `datfile.Terrain` (full nested shape in `api_schemas.json`)

### `dat terrain-patch`

```
kit dat terrain-patch <in.dat> <out.dat> <terrain_id> [--name TEXT] [--name-2 TEXT] [--overlay-mask-name TEXT]
```

| flag | value |
| --- | --- |
| `--name` | `TEXT` |
| `--name-2` | `TEXT` |
| `--overlay-mask-name` | `TEXT` |

**writes output files**

- probe: `skipped_mutating`

### `dat terrain-restriction-patch`

```
kit dat terrain-restriction-patch <in.dat> <out.dat> <restriction_id> --terrain TERRAIN_ID,PASSABILITY
```

| flag | value |
| --- | --- |
| `--terrain` | `TERRAIN_ID,PASSABILITY` **(required)** |

**writes output files**

- probe: `skipped_mutating`

### `dat terrain-restrictions`

```
kit dat terrain-restrictions <empires*.dat> [--limit N|--all]
```

| flag | value |
| --- | --- |
| `--limit` | `N|--all` |

- probe: `ok`
- JSON top-level keys: `restriction_count`, `returned_count`, `terrain_restrictions`, `terrain_row_count`, `truncated`, `version`

### `dat terrains`

```
kit dat terrains <empires*.dat> [--limit N|--all]
```

| flag | value |
| --- | --- |
| `--limit` | `N|--all` |

- probe: `ok`
- JSON top-level keys: `returned_count`, `terrain_count`, `terrains`, `truncated`, `version`

### `dat unit`

```
kit dat unit <empires*.dat> <civ_id> <unit_id>
```

- probe: `ok`
- JSON top-level keys: `action`, `attributes`, `blood_unit_id`, `civ_index`, `class`, `creatable`, `dying_graphic`, `enabled`, `field_spans`, `hit_points`, `icon_id`, `id`, `index`, `line_of_sight`, `list_spans`, `movement_type`, `name`, `present`, `record_end`, `record_length`, `record_start`, `standing_graphic_1`, `standing_graphic_2`, `type`, `type50`
- response type: `datfile.UnitSummary` (full nested shape in `api_schemas.json`)

### `dat unit-child-delete`

```
kit dat unit-child-delete <in.dat> <out.dat> <damage-graphic|attack|armour|train-location|drop-site|task> <civ_id> <unit_id> <row_index>
```

**writes output files**

- probe: `skipped_mutating`

### `dat unit-create`

```
kit dat unit-create <in.dat> <out.dat> --from-civ N --from-unit N (--civ N|--all-civs) [unit patch flags]
```

| flag | value |
| --- | --- |
| `--from-civ` | `N` **(required)** |
| `--from-unit` | `N` **(required)** |

**writes output files**

- probe: `skipped_mutating`

### `dat unit-delete`

```
kit dat unit-delete <in.dat> <out.dat> <unit_id> (--civ N|--all-civs)
```

**writes output files**

- probe: `skipped_mutating`

### `dat unit-header`

```
kit dat unit-header <empires*.dat> <header_id>
```

- probe: `ok`
- JSON top-level keys: `header_id`, `unit_header`, `version`

### `dat unit-header-task-delete`

```
kit dat unit-header-task-delete <in.dat> <out.dat> <unit_header_id> <task_index>
```

**writes output files**

- probe: `skipped_mutating`

### `dat unit-headers`

```
kit dat unit-headers <empires*.dat> [--limit N|--all]
```

| flag | value |
| --- | --- |
| `--limit` | `N|--all` |

- probe: `ok`
- JSON top-level keys: `header_count`, `present_count`, `returned_count`, `task_count`, `truncated`, `unit_headers`, `version`

### `dat units`

```
kit dat units <empires*.dat> [--civ N] [--id N] [--name TEXT|--name-contains TEXT] [--class building|unit|creatable] [--limit N|--all] [--json]
```

| flag | value |
| --- | --- |
| `--civ` | `N` |
| `--id` | `N` |
| `--name` | `TEXT|--name-contains` |
| `--class` | `building|unit|creatable` |
| `--limit` | `N|--all` |
| `--json` | _(boolean)_ |

- probe: `ok`
- JSON top-level keys: `civ_count`, `filters`, `present_count`, `returned_count`, `truncated`, `units`, `version`

## docs

### `docs lint`

```
kit docs lint [folder] [--text|--json]
```

- probe: `ok`
- verification: `structure_verified_not_engine_verified`
- JSON top-level keys: `examples`, `files_scanned`, `ok`, `root`, `verification`

## fx

### `fx bind`

```
kit fx bind --atlas file.png|--dds file.dds --grid RxC|--frames N --frame-size WxH --into resources/_common --dat empires*.dat --unit N --slot flying|standing|attack|dying --from graphic_id --name fx_name [--dry-run]
```

| flag | value |
| --- | --- |
| `--atlas` | `file.png|--dds` **(required)** |
| `--grid` | `RxC|--frames` **(required)** |
| `--frame-size` | `WxH` **(required)** |
| `--into` | `resources/_common` **(required)** |
| `--dat` | `empires*.dat` **(required)** |
| `--unit` | `N` **(required)** |
| `--slot` | `flying|standing|attack|dying` **(required)** |
| `--from` | `graphic_id` **(required)** |
| `--name` | `fx_name` **(required)** |
| `--dry-run` | _(boolean)_ |

**writes output files**

- probe: `skipped_mutating`

### `fx lint`

```
kit fx lint <mod-or-resources/_common> [--dat empires*.dat] [--text]
```

| flag | value |
| --- | --- |
| `--dat` | `empires*.dat` |
| `--text` | _(boolean)_ |

- probe: `non_json_output`
- note: kit fx lint: read particles dir: open <empires2_x2_p1.dat>/particles: not a directory

### `fx new`

```
kit fx new <name> --preset trail|explosion|aura|projectile-fire [--grid RxC] [descriptor overrides]
```

| flag | value |
| --- | --- |
| `--preset` | `trail|explosion|aura|projectile-fire` **(required)** |
| `--grid` | `RxC` |

**writes output files**

- probe: `skipped_mutating`

## gfx

### `gfx export`

```
kit gfx export <file.sld> [--out dir] [--limit N] [--text]
```

| flag | value |
| --- | --- |
| `--out` | `dir` |
| `--limit` | `N` |
| `--text` | _(boolean)_ |

**writes output files**

- probe: `skipped_mutating`

### `gfx info`

```
kit gfx info <file.sld> [--text]
```

| flag | value |
| --- | --- |
| `--text` | _(boolean)_ |

- probe: `skipped_no_fixture`

## player

### `player stats`

```
kit player stats <profileId> [--match-type N] [--cache-dir DIR] [--force] [--text]
```

| flag | value |
| --- | --- |
| `--match-type` | `N` |
| `--cache-dir` | `DIR` |
| `--force` | _(boolean)_ |
| `--text` | _(boolean)_ |

**network I/O**

- probe: `skipped_network`

## project

### `project diff`

```
kit project diff <before-snapshot.json> <after-snapshot.json> [--limit N] [--text|--json]
```

| flag | value |
| --- | --- |
| `--limit` | `N` |

- probe: `skipped_no_fixture`

### `project inspect`

```
kit project inspect [folder] [--limit N] [--text|--json]
```

| flag | value |
| --- | --- |
| `--limit` | `N` |

- probe: `ok`
- verification: `structure_verified_not_engine_verified`
- JSON top-level keys: `files`, `files_scanned`, `kind_counts`, `recommendations`, `role_counts`, `root`, `shown`, `verification`
- response type: `kit.ProjectInspectReport` (full nested shape in `api_schemas.json`)

### `project lineage`

```
kit project lineage --input PATH --output PATH [--input PATH...] [--output PATH...] [--tool TEXT] [--note TEXT] [--manifest lineage.json] [--text|--json]
```

| flag | value |
| --- | --- |
| `--input` | `PATH` **(required)** |
| `--output` | `PATH` **(required)** |
| `--input` | `PATH...` |
| `--output` | `PATH...` |
| `--tool` | `TEXT` |
| `--note` | `TEXT` |
| `--manifest` | `lineage.json` |

**writes output files**

- probe: `skipped_mutating`

### `project snapshot`

```
kit project snapshot [folder] --out project-snapshot.json [--text|--json]
```

| flag | value |
| --- | --- |
| `--out` | `project-snapshot.json` **(required)** |

**writes output files**

- probe: `skipped_mutating`

## recipe

### `recipe export`

```
kit recipe export <out-dir> [--domain scenario|dat|all] [--force] [--text|--json]
```

| flag | value |
| --- | --- |
| `--domain` | `scenario|dat|all` |
| `--force` | _(boolean)_ |

**writes output files**

- probe: `skipped_mutating`

### `recipe list`

```
kit recipe list [--domain scenario|dat|all] [--text|--json]
```

| flag | value |
| --- | --- |
| `--domain` | `scenario|dat|all` |

- probe: `ok`
- verification: `structure_verified_not_engine_verified`
- JSON top-level keys: `count`, `templates`, `verification`
- response type: `kit.RecipeTemplateListReport` (full nested shape in `api_schemas.json`)

### `recipe show`

```
kit recipe show <name> [--recipe-only] [--text|--json]
```

| flag | value |
| --- | --- |
| `--recipe-only` | _(boolean)_ |

- probe: `non_json_output`
- note: usage: kit recipe show <name> [--recipe-only] [--text|--json]

## replay

### `replay actions`

```
kit replay actions <file.aoe2record> [--action-id N] [--limit N] [--objects] [--raw] [--text|--json]
```

| flag | value |
| --- | --- |
| `--action-id` | `N` |
| `--limit` | `N` |
| `--objects` | _(boolean)_ |
| `--raw` | _(boolean)_ |

- probe: `skipped_no_fixture`

### `replay ai-manifest`

```
kit replay ai-manifest <file.aoe2record> [--promisory-root DIR] [--text]
```

| flag | value |
| --- | --- |
| `--promisory-root` | `DIR` |
| `--text` | _(boolean)_ |

- probe: `skipped_no_fixture`

### `replay carrier`

```
kit replay carrier <file.aoe2record> --ledger expected.json [--text|--json]
```

| flag | value |
| --- | --- |
| `--ledger` | `expected.json` **(required)** |

- probe: `requires_additional_input`
- note: required --ledger must be supplied by the caller

### `replay chat`

```
kit replay chat <file.aoe2record> [--text|--json]
```

- probe: `skipped_no_fixture`

### `replay checksum-phase`

```
kit replay checksum-phase <file.aoe2record> [--word N|--all-words] [--player N] [--text|--json]
```

| flag | value |
| --- | --- |
| `--word` | `N|--all-words` |
| `--player` | `N` |

- probe: `skipped_no_fixture`

### `replay checksum-probe`

```
kit replay checksum-probe <file.aoe2record> [--preset v6-playground-helper|v7-checksum-calibration] [--player N] [--object-count-delta N] [--unit-type-sum-delta N] [--object-id-sum-delta N] [--state-sum-delta N] [--carry-sum-delta N] [--digest-delta N] [--limit N] [--text|--json]
```

| flag | value |
| --- | --- |
| `--preset` | `v6-playground-helper|v7-checksum-calibration` |
| `--player` | `N` |
| `--object-count-delta` | `N` |
| `--unit-type-sum-delta` | `N` |
| `--object-id-sum-delta` | `N` |
| `--state-sum-delta` | `N` |
| `--carry-sum-delta` | `N` |
| `--digest-delta` | `N` |
| `--limit` | `N` |

- probe: `skipped_no_fixture`

### `replay combat`

```
kit replay combat <file.aoe2record> [--window-ms N] [--min-net-loss N] [--limit N] [--sort loss|time] [--text|--json]
```

| flag | value |
| --- | --- |
| `--window-ms` | `N` |
| `--min-net-loss` | `N` |
| `--limit` | `N` |
| `--sort` | `loss|time` |

- probe: `skipped_no_fixture`

### `replay corpus`

```
kit replay corpus <folder-or-replay> [--flat] [--no-zip] [--unknown-samples N] [--text]
```

| flag | value |
| --- | --- |
| `--flat` | _(boolean)_ |
| `--no-zip` | _(boolean)_ |
| `--unknown-samples` | `N` |
| `--text` | _(boolean)_ |

- probe: `ok`
- method: `folder_replay_corpus_coverage_events_postgame`
- verification: `structure_verified_not_engine_verified`
- JSON top-level keys: `folder`, `method`, `rows`, `summary`, `verification`
- response type: `replay.ReplayCorpusReport` (full nested shape in `api_schemas.json`)

### `replay coverage`

```
kit replay coverage <file.aoe2record> [--text|--json] [--fail-on-opaque]
```

| flag | value |
| --- | --- |
| `--fail-on-opaque` | _(boolean)_ |

- probe: `skipped_no_fixture`

### `replay datamod-check`

```
kit replay datamod-check <file.aoe2record> [--baseline-replay baseline.aoe2record] [--dat empires2_x2_p1.dat] [--known-template SHA256] [--tail-offset N] [--count N] [--limit N] [--text]
```

| flag | value |
| --- | --- |
| `--baseline-replay` | `baseline.aoe2record` |
| `--dat` | `empires2_x2_p1.dat` |
| `--known-template` | `SHA256` |
| `--tail-offset` | `N` |
| `--count` | `N` |
| `--limit` | `N` |
| `--text` | _(boolean)_ |

- probe: `skipped_no_fixture`

### `replay deaths`

```
kit replay deaths <file.aoe2record> [--limit N] [--text|--json]
```

| flag | value |
| --- | --- |
| `--limit` | `N` |

- probe: `skipped_no_fixture`

### `replay diff-state`

```
kit replay diff-state <before.aoe2record> <after.aoe2record> [--focus all|opaque] [--limit N] [--text]
```

| flag | value |
| --- | --- |
| `--focus` | `all|opaque` |
| `--limit` | `N` |
| `--text` | _(boolean)_ |

- probe: `skipped_no_fixture`

### `replay diff-triggers`

```
kit replay diff-triggers <before.aoe2record|zip> <after.aoe2record|zip> [--limit N] [--text]
```

| flag | value |
| --- | --- |
| `--limit` | `N` |
| `--text` | _(boolean)_ |

- probe: `skipped_no_fixture`

### `replay effective-data`

```
kit replay effective-data <file.aoe2record> [--dat empires2_x2_p1.dat] [--player N] [--tail-offset N] [--count N] [--limit N] [--known-template SHA256] [--all] [--text|--tree]
```

| flag | value |
| --- | --- |
| `--dat` | `empires2_x2_p1.dat` |
| `--player` | `N` |
| `--tail-offset` | `N` |
| `--count` | `N` |
| `--limit` | `N` |
| `--known-template` | `SHA256` |
| `--all` | _(boolean)_ |

- probe: `skipped_no_fixture`

### `replay effective-units`

```
kit replay effective-units <file.aoe2record> --dat empires2_x2_p1.dat [--player N] [--limit N] [--include-unchanged] [--all-types] [--include-duplicates] [--text]
```

| flag | value |
| --- | --- |
| `--dat` | `empires2_x2_p1.dat` **(required)** |
| `--player` | `N` |
| `--limit` | `N` |
| `--include-unchanged` | _(boolean)_ |
| `--all-types` | _(boolean)_ |
| `--include-duplicates` | _(boolean)_ |
| `--text` | _(boolean)_ |

- probe: `skipped_no_fixture`

### `replay events`

```
kit replay events <file.aoe2record> [--all] [--system] [--untyped-actions] [--objects] [--raw] [--context context.json] [--text]
```

| flag | value |
| --- | --- |
| `--all` | _(boolean)_ |
| `--system` | _(boolean)_ |
| `--untyped-actions` | _(boolean)_ |
| `--objects` | _(boolean)_ |
| `--raw` | _(boolean)_ |
| `--context` | `context.json` |
| `--text` | _(boolean)_ |

- probe: `skipped_no_fixture`

### `replay feedback`

```
kit replay feedback <file.aoe2record> [--text]
```

| flag | value |
| --- | --- |
| `--text` | _(boolean)_ |

- probe: `skipped_no_fixture`

### `replay fetch`

```
kit replay fetch <gameId> --all-povs --from-roster <replay.aoe2record|zip> [--out dir] [--force] [--text]
```

| flag | value |
| --- | --- |
| `--all-povs` | _(boolean)_ **(required)** |
| `--from-roster` | _(boolean)_ **(required)** |
| `--out` | `dir` |
| `--force` | _(boolean)_ |
| `--text` | _(boolean)_ |

**network I/O**

- probe: `skipped_network`

### `replay frontier`

```
kit replay frontier <file.aoe2record> [--min-duplicate-bytes N] [--limit N] [--text|--json]
```

| flag | value |
| --- | --- |
| `--min-duplicate-bytes` | `N` |
| `--limit` | `N` |

- probe: `skipped_no_fixture`

### `replay header-anchors`

```
kit replay header-anchors <file.aoe2record> [--limit N] [--text|--json]
```

| flag | value |
| --- | --- |
| `--limit` | `N` |

- probe: `skipped_no_fixture`

### `replay inbox`

```
kit replay inbox <folder> [--context context.json] [--text]
```

| flag | value |
| --- | --- |
| `--context` | `context.json` |
| `--text` | _(boolean)_ |

- probe: `ok`
- JSON top-level keys: `count`, `items`, `root`, `summary`
- response type: `replay.InboxReport` (full nested shape in `api_schemas.json`)

### `replay info`

```
kit replay info <file.aoe2record|zip> [--text|--json] [--tiles] [--full]
```

| flag | value |
| --- | --- |
| `--tiles` | _(boolean)_ |
| `--full` | _(boolean)_ |

- probe: `skipped_no_fixture`

### `replay issues`

```
kit replay issues <file.aoe2record> [--context context.json] [--text]
```

| flag | value |
| --- | --- |
| `--context` | `context.json` |
| `--text` | _(boolean)_ |

- probe: `skipped_no_fixture`

### `replay lifecycle`

```
kit replay lifecycle <file.aoe2record> [--text]
```

| flag | value |
| --- | --- |
| `--text` | _(boolean)_ |

- probe: `skipped_no_fixture`

### `replay object-shapes`

```
kit replay object-shapes <file.aoe2record> [--referenced] [--limit N] [--text]
```

| flag | value |
| --- | --- |
| `--referenced` | _(boolean)_ |
| `--limit` | `N` |
| `--text` | _(boolean)_ |

- probe: `skipped_no_fixture`

### `replay object-state`

```
kit replay object-state <file.aoe2record> [--referenced] [--class CLASS] [--limit N] [--text]
```

| flag | value |
| --- | --- |
| `--referenced` | _(boolean)_ |
| `--class` | `CLASS` |
| `--limit` | `N` |
| `--text` | _(boolean)_ |

- probe: `skipped_no_fixture`

### `replay objects`

```
kit replay objects <file.aoe2record> [--referenced] [--limit N] [--text]
```

| flag | value |
| --- | --- |
| `--referenced` | _(boolean)_ |
| `--limit` | `N` |
| `--text` | _(boolean)_ |

- probe: `skipped_no_fixture`

### `replay opaque-clusters`

```
kit replay opaque-clusters <folder-or-replay> [--space inflated_header|body] [--min-bytes N] [--limit N] [--samples N] [--flat] [--no-zip] [--text]
```

| flag | value |
| --- | --- |
| `--space` | `inflated_header|body` |
| `--min-bytes` | `N` |
| `--limit` | `N` |
| `--samples` | `N` |
| `--flat` | _(boolean)_ |
| `--no-zip` | _(boolean)_ |
| `--text` | _(boolean)_ |

- probe: `ok`
- method: `folder_opaque_span_structural_clustering`
- verification: `structure_verified_opaque_bytes_profiled_not_semantically_decoded`
- JSON top-level keys: `clusters`, `folder`, `method`, `summary`, `verification`, `warnings`
- response type: `replay.OpaqueClusterReport` (full nested shape in `api_schemas.json`)

### `replay opaque-spans`

```
kit replay opaque-spans <file.aoe2record> [--space inflated_header|body] [--min-bytes N] [--limit N] [--text]
```

| flag | value |
| --- | --- |
| `--space` | `inflated_header|body` |
| `--min-bytes` | `N` |
| `--limit` | `N` |
| `--text` | _(boolean)_ |

- probe: `skipped_no_fixture`

### `replay player-events`

```
kit replay player-events <file.aoe2record> [--player N] [--type TYPE] [--action-id N] [--phase PHASE] [--from TIME] [--to TIME] [--limit N] [--objects] [--raw] [--text]
```

| flag | value |
| --- | --- |
| `--player` | `N` |
| `--type` | `TYPE` |
| `--action-id` | `N` |
| `--phase` | `PHASE` |
| `--from` | `TIME` |
| `--to` | `TIME` |
| `--limit` | `N` |
| `--objects` | _(boolean)_ |
| `--raw` | _(boolean)_ |
| `--text` | _(boolean)_ |

- probe: `skipped_no_fixture`

### `replay player-profile`

```
kit replay player-profile <file.aoe2record> [--context context.json] [--window-sec N] [--dead-gap-sec N] [--cell-size N] [--include-events] [--text|--json]
```

| flag | value |
| --- | --- |
| `--context` | `context.json` |
| `--window-sec` | `N` |
| `--dead-gap-sec` | `N` |
| `--cell-size` | `N` |
| `--include-events` | _(boolean)_ |

- probe: `skipped_no_fixture`

### `replay player-series`

```
kit replay player-series <file.aoe2record> [--player N] [--changes-only] [--limit N] [--text|--json]
```

| flag | value |
| --- | --- |
| `--player` | `N` |
| `--changes-only` | _(boolean)_ |
| `--limit` | `N` |

- probe: `skipped_no_fixture`

### `replay playtest`

```
kit replay playtest <file.aoe2record> [--window N] [--include-computers] [--text|--json]
```

| flag | value |
| --- | --- |
| `--window` | `N` |
| `--include-computers` | _(boolean)_ |

- probe: `skipped_no_fixture`

### `replay postgame`

```
kit replay postgame <file.aoe2record> [--text]
```

| flag | value |
| --- | --- |
| `--text` | _(boolean)_ |

- probe: `skipped_no_fixture`

### `replay scan-value`

```
kit replay scan-value <file.aoe2record> [--value N] [--width 8|16|32|64] [--hex BYTES] [--string TEXT] [--space inflated_header|body] [--limit N] [--text]
```

| flag | value |
| --- | --- |
| `--value` | `N` |
| `--width` | `8|16|32|64` |
| `--hex` | `BYTES` |
| `--string` | `TEXT` |
| `--space` | `inflated_header|body` |
| `--limit` | `N` |
| `--text` | _(boolean)_ |

- probe: `skipped_no_fixture`

### `replay sidecar-sync`

```
kit replay sidecar-sync <file.aoe2record> --xsdat run.xsdat (--ledger expected.json|--schema rtv12|rtv13|rtv14|rtv141|rtv142|rtv15|rtv16|rtv17|a2ksem2) [--player N] [--window-ms N] [--fail-on-mismatch] [--text|--json]
```

| flag | value |
| --- | --- |
| `--xsdat` | `run.xsdat` **(required)** |
| `--player` | `N` |
| `--window-ms` | `N` |
| `--fail-on-mismatch` | _(boolean)_ |

- probe: `requires_additional_input`
- note: required --xsdat must be supplied by the caller

### `replay spawns`

```
kit replay spawns <file.aoe2record> [--text]
```

| flag | value |
| --- | --- |
| `--text` | _(boolean)_ |

- probe: `skipped_no_fixture`

### `replay story`

```
kit replay story <file.aoe2record> [--context context.json] [--text|--brief]
```

| flag | value |
| --- | --- |
| `--context` | `context.json` |

- probe: `skipped_no_fixture`

### `replay sync`

```
kit replay sync <file.aoe2record> [--checksums] [--raw-words] [--limit N] [--text|--json]
```

| flag | value |
| --- | --- |
| `--checksums` | _(boolean)_ |
| `--raw-words` | _(boolean)_ |
| `--limit` | `N` |

- probe: `skipped_no_fixture`

### `replay sync-log`

```
kit replay sync-log <p0-sync.txt> [--replay file.aoe2record] [--limit N] [--text|--json]
```

| flag | value |
| --- | --- |
| `--replay` | `file.aoe2record` |
| `--limit` | `N` |

- probe: `skipped_no_fixture`

### `replay telemetry`

```
kit replay telemetry <file-or-folder> [--context context.json] [--schema telemetry.json] [--text]
```

| flag | value |
| --- | --- |
| `--context` | `context.json` |
| `--schema` | `telemetry.json` |
| `--text` | _(boolean)_ |

- probe: `skipped_no_fixture`

### `replay trigger-neighborhood`

```
kit replay trigger-neighborhood <file.aoe2record|zip> --trigger N [--depth N] [--limit N] [--text]
```

| flag | value |
| --- | --- |
| `--trigger` | `N` **(required)** |
| `--depth` | `N` |
| `--limit` | `N` |
| `--text` | _(boolean)_ |

- probe: `requires_additional_input`
- note: required --trigger must be supplied by the caller

### `replay triggers`

```
kit replay triggers <file.aoe2record|zip> [--grep TEXT] [--effect ID_OR_NAME] [--condition ID_OR_NAME] [--player N] [--variable N] [--unit-const ID] [--limit N] [--text]
```

| flag | value |
| --- | --- |
| `--grep` | `TEXT` |
| `--effect` | `ID_OR_NAME` |
| `--condition` | `ID_OR_NAME` |
| `--player` | `N` |
| `--variable` | `N` |
| `--unit-const` | `ID` |
| `--limit` | `N` |
| `--text` | _(boolean)_ |

- probe: `skipped_no_fixture`

### `replay unknowns`

```
kit replay unknowns <folder> [--text]
```

| flag | value |
| --- | --- |
| `--text` | _(boolean)_ |

- probe: `ok`
- JSON top-level keys: `replays`, `root`, `warnings`
- response type: `replay.UnknownsReport` (full nested shape in `api_schemas.json`)

### `replay xs-telemetry`

```
kit replay xs-telemetry <file.aoe2record> [--text]
```

| flag | value |
| --- | --- |
| `--text` | _(boolean)_ |

- probe: `skipped_no_fixture`

## roadmap

### `roadmap crud`

```
kit roadmap crud [--domain scenario|dat|replay|all] [--text|--json]
```

| flag | value |
| --- | --- |
| `--domain` | `scenario|dat|replay|all` |

- probe: `ok`
- method: `code-owned CRUD capability ledger derived from the R/W/D source rows; update is the authoring write surface`
- verification: `structure_verified_not_engine_verified`
- JSON top-level keys: `domain`, `method`, `notes`, `rows`, `summary`, `verification`, `version`
- response type: `roadmap.CRUDReport` (full nested shape in `api_schemas.json`)

### `roadmap dark`

```
kit roadmap dark [--domain scenario|dat|replay|all] [--text|--json]
```

| flag | value |
| --- | --- |
| `--domain` | `scenario|dat|replay|all` |

- probe: `ok`
- method: `frontier ledger from decoded replay/scenario/DAT work; unknowns stay named instead of becoming claims`
- verification: `structure_verified_not_engine_verified`
- JSON top-level keys: `domain`, `frontiers`, `method`, `notes`, `summary`, `verification`, `version`
- response type: `roadmap.DarkReport` (full nested shape in `api_schemas.json`)

### `roadmap rwd`

```
kit roadmap rwd [--domain scenario|dat|replay|all] [--text|--json]
```

| flag | value |
| --- | --- |
| `--domain` | `scenario|dat|replay|all` |

- probe: `ok`
- method: `code-owned capability ledger; statuses describe implemented Kit surfaces, not promised engine behavior`
- verification: `structure_verified_not_engine_verified`
- JSON top-level keys: `domain`, `method`, `notes`, `rows`, `summary`, `verification`, `version`
- response type: `roadmap.MatrixReport` (full nested shape in `api_schemas.json`)

## rpg

### `rpg shop-demo`

```
kit rpg shop-demo [--out-dir DIR] [--name TEXT] [--timestamp UNIX] [--text|--json]
```

| flag | value |
| --- | --- |
| `--out-dir` | `DIR` |
| `--name` | `TEXT` |
| `--timestamp` | `UNIX` |

**writes output files**

- probe: `skipped_mutating`

## scen

### `scen analyze`

```
kit scen analyze <file.aoe2scenario>
```

- probe: `skipped_no_fixture`

### `scen audit-player-coverage`

```
kit scen audit-player-coverage <file.aoe2scenario> [--players 1,2,3,4] [--text]
```

| flag | value |
| --- | --- |
| `--players` | `1,2,3,4` |
| `--text` | _(boolean)_ |

- probe: `skipped_no_fixture`

### `scen blank`

```
kit scen blank <out.aoe2scenario> [--size N] [--width N] [--height N] [--players N] [--human-slots N] [--timestamp UNIX] [--dummy-starters] [--dummy-unit UNIT_ID] [--dummy-origin X,Y] [--dummy-spacing N] [--no-conquest] [--keep-seed-triggers] [--text]
```

| flag | value |
| --- | --- |
| `--size` | `N` |
| `--width` | `N` |
| `--height` | `N` |
| `--players` | `N` |
| `--human-slots` | `N` |
| `--timestamp` | `UNIX` |
| `--dummy-starters` | _(boolean)_ |
| `--dummy-unit` | `UNIT_ID` |
| `--dummy-origin` | `X,Y` |
| `--dummy-spacing` | `N` |
| `--no-conquest` | _(boolean)_ |
| `--keep-seed-triggers` | _(boolean)_ |
| `--text` | _(boolean)_ |

**writes output files**

- probe: `skipped_mutating`

### `scen delete`

```
kit scen delete <in.aoe2scenario> <out.aoe2scenario> units-area <x1,y1,x2,y2> [--player N] [--unit N]
```

| flag | value |
| --- | --- |
| `--player` | `N` |
| `--unit` | `N` |

**writes output files**

- probe: `skipped_no_fixture`
- verification: `structure_verified_not_engine_verified`
- JSON top-level keys: `delete_plan`, `patch_report`, `request`, `verification`, `version`

### `scen delete-plan`

```
kit scen delete-plan <file.aoe2scenario> units-area <x1,y1,x2,y2> [--player N] [--unit N] [--text]
```

| flag | value |
| --- | --- |
| `--player` | `N` |
| `--unit` | `N` |
| `--text` | _(boolean)_ |

- probe: `skipped_no_fixture`

### `scen deploycheck`

```
kit scen deploycheck <file.aoe2scenario> <deploy-tree> [--text] [--include-provisional]
```

| flag | value |
| --- | --- |
| `--text` | _(boolean)_ |
| `--include-provisional` | _(boolean)_ |

- probe: `skipped_no_fixture`

### `scen describe`

```
kit scen describe <file.aoe2scenario> [--section triggers|units|map|players|victory|messages|disables|ai|resources|all] [--full] [--json] [--no-path|--quiet-header]
```

| flag | value |
| --- | --- |
| `--section` | `triggers|units|map|players|victory|messages|disables|ai|resources|all` |
| `--full` | _(boolean)_ |
| `--json` | _(boolean)_ |

- probe: `skipped_no_fixture`

### `scen diff`

```
kit scen diff <before.aoe2scenario> <after.aoe2scenario> [--text|--json]
```

- probe: `skipped_no_fixture`

### `scen diff-triggers`

```
kit scen diff-triggers <before.aoe2scenario> <after.aoe2scenario> [--limit N] [--text|--json]
```

| flag | value |
| --- | --- |
| `--limit` | `N` |

- probe: `skipped_no_fixture`

### `scen disconnect`

```
kit scen disconnect <in.aoe2scenario> <out.aoe2scenario> units-area <x1,y1,x2,y2> [--player N] [--unit N]
```

| flag | value |
| --- | --- |
| `--player` | `N` |
| `--unit` | `N` |

**writes output files**

- probe: `skipped_no_fixture`
- verification: `structure_verified_not_engine_verified`
- JSON top-level keys: `patch_report`, `reference_summary`, `request`, `verification`, `version`

### `scen effects`

```
kit scen effects <file.aoe2scenario> [--raw|--census|--where EFFECT] [--limit N] [--max-mb N] [--text]
```

| flag | value |
| --- | --- |
| `--limit` | `N` |
| `--max-mb` | `N` |
| `--text` | _(boolean)_ |

- probe: `skipped_no_fixture`

### `scen glossary`

```
kit scen glossary <file.aoe2scenario> [--limit N] [--max-mb N] [--text]
```

| flag | value |
| --- | --- |
| `--limit` | `N` |
| `--max-mb` | `N` |
| `--text` | _(boolean)_ |

- probe: `skipped_no_fixture`

### `scen idioms`

```
kit scen idioms <file.aoe2scenario> [--text]
```

| flag | value |
| --- | --- |
| `--text` | _(boolean)_ |

- probe: `skipped_no_fixture`

### `scen lint`

```
kit scen lint <file.aoe2scenario> [--text] [--include-provisional]
```

| flag | value |
| --- | --- |
| `--text` | _(boolean)_ |
| `--include-provisional` | _(boolean)_ |

- probe: `skipped_no_fixture`

### `scen mechanic`

```
kit scen mechanic <file.aoe2scenario> --kind garrison-token|transform-toggle|teleport-transition|refresh-cycle [--grep TEXT] [--text]
```

| flag | value |
| --- | --- |
| `--kind` | `garrison-token|transform-toggle|teleport-transition|refresh-cycle` **(required)** |
| `--grep` | `TEXT` |
| `--text` | _(boolean)_ |

- probe: `requires_additional_input`
- note: required --kind must be supplied by the caller

### `scen palette-usage`

```
kit scen palette-usage <file.aoe2scenario> --dat <empires*.dat> [--json|--text]
```

| flag | value |
| --- | --- |
| `--dat` | _(boolean)_ **(required)** |

- probe: `skipped_no_fixture`

### `scen patch`

```
kit scen patch <in.aoe2scenario> <out.aoe2scenario> --recipe recipe.json
```

| flag | value |
| --- | --- |
| `--recipe` | `recipe.json` **(required)** |

**writes output files**

- probe: `skipped_mutating`

### `scen plan`

```
kit scen plan <in.aoe2scenario> --recipe recipe.json
```

| flag | value |
| --- | --- |
| `--recipe` | `recipe.json` **(required)** |

**writes output files**

- probe: `skipped_mutating`

### `scen refs`

```
kit scen refs <file.aoe2scenario> [--kind unit|trigger|variable|string|all] [--id N] [--text]
```

| flag | value |
| --- | --- |
| `--kind` | `unit|trigger|variable|string|all` |
| `--id` | `N` |
| `--text` | _(boolean)_ |

- probe: `skipped_no_fixture`

### `scen settings`

```
kit scen settings <file.aoe2scenario> [--text|--json]
```

- probe: `skipped_no_fixture`

### `scen smoke`

```
kit scen smoke <in.aoe2scenario> <out.aoe2scenario> [--x N] [--y N] [--player N] [--unit N] [--terrain N] [--elevation N] [--layer N]
```

| flag | value |
| --- | --- |
| `--x` | `N` |
| `--y` | `N` |
| `--player` | `N` |
| `--unit` | `N` |
| `--terrain` | `N` |
| `--elevation` | `N` |
| `--layer` | `N` |

**writes output files**

- probe: `skipped_mutating`

### `scen smoke-recipe`

```
kit scen smoke-recipe [--x N] [--y N] [--player N] [--unit N] [--terrain N] [--elevation N] [--layer N]
```

| flag | value |
| --- | --- |
| `--x` | `N` |
| `--y` | `N` |
| `--player` | `N` |
| `--unit` | `N` |
| `--terrain` | `N` |
| `--elevation` | `N` |
| `--layer` | `N` |

- probe: `ok`
- JSON top-level keys: `map`, `triggers`, `units`
- response type: `scenario.Recipe` (full nested shape in `api_schemas.json`)

### `scen strings`

```
kit scen strings <file.aoe2scenario>
```

- probe: `skipped_no_fixture`

### `scen terrain`

```
kit scen terrain <file.aoe2scenario> [--id N] [--tiles] [--text|--json]
```

| flag | value |
| --- | --- |
| `--id` | `N` |
| `--tiles` | _(boolean)_ |

- probe: `skipped_no_fixture`

### `scen trigger-flow`

```
kit scen trigger-flow <file.aoe2scenario> (--grep TEXT|--unit-ref ID|--variable N) [--limit N] [--text]
```

| flag | value |
| --- | --- |
| `--limit` | `N` |
| `--text` | _(boolean)_ |

- probe: `skipped_no_fixture`

### `scen trigger-neighborhood`

```
kit scen trigger-neighborhood <file.aoe2scenario> (--trigger N|--unit-ref ID|--variable N) [--depth N] [--limit N] [--text]
```

| flag | value |
| --- | --- |
| `--depth` | `N` |
| `--limit` | `N` |
| `--text` | _(boolean)_ |

- probe: `skipped_no_fixture`

### `scen triggers`

```
kit scen triggers <file.aoe2scenario> [--grep TEXT] [--effect ID_OR_NAME] [--condition ID_OR_NAME] [--message TEXT] [--unit-ref ID] [--unit-type ID] [--player N] [--variable N] [--area x1,y1,x2,y2] [--limit N] [--max-mb N] [--text|--json]
```

| flag | value |
| --- | --- |
| `--grep` | `TEXT` |
| `--effect` | `ID_OR_NAME` |
| `--condition` | `ID_OR_NAME` |
| `--message` | `TEXT` |
| `--unit-ref` | `ID` |
| `--unit-type` | `ID` |
| `--player` | `N` |
| `--variable` | `N` |
| `--area` | `x1,y1,x2,y2` |
| `--limit` | `N` |
| `--max-mb` | `N` |

- probe: `skipped_no_fixture`

### `scen units`

```
kit scen units <file.aoe2scenario> [--named]
```

| flag | value |
| --- | --- |
| `--named` | _(boolean)_ |

- probe: `skipped_no_fixture`

### `scen write-check`

```
kit scen write-check <before.aoe2scenario> <after.aoe2scenario> [--text|--json] [--include-provisional]
```

| flag | value |
| --- | --- |
| `--include-provisional` | _(boolean)_ |

- probe: `skipped_no_fixture`

### `scen xs`

```
kit scen xs <file.aoe2scenario> [--deploy-tree dir] [--text|--json]
```

| flag | value |
| --- | --- |
| `--deploy-tree` | `dir` |

- probe: `skipped_no_fixture`

### `scen xs attach`

```
kit scen xs attach <in.aoe2scenario> <out.aoe2scenario> --xs file.xs [--name Entry.xs] [--also-carrier] [--title TITLE] [--trigger-name NAME] [--text|--json]
```

| flag | value |
| --- | --- |
| `--xs` | `file.xs` **(required)** |
| `--name` | `Entry.xs` |
| `--also-carrier` | _(boolean)_ |
| `--title` | `TITLE` |
| `--trigger-name` | `NAME` |

**writes output files**

- probe: `skipped_mutating`

### `scen xs compare`

```
kit scen xs compare <file.aoe2scenario> <file.xs> [--carrier N|--trigger N] [--text|--json]
```

| flag | value |
| --- | --- |
| `--carrier` | `N|--trigger` |

- probe: `skipped_no_fixture`

### `scen xs deploy`

```
kit scen xs deploy <file.aoe2scenario> <deploy-tree> [--name Entry.xs] [--carrier N|--trigger N] [--force] [--check] [--text|--json]
```

| flag | value |
| --- | --- |
| `--name` | `Entry.xs` |
| `--carrier` | `N|--trigger` |
| `--force` | _(boolean)_ |
| `--check` | _(boolean)_ |

**writes output files**

- probe: `skipped_mutating`

### `scen xs embed`

```
kit scen xs embed <in.aoe2scenario> <out.aoe2scenario> --xs file.xs [--title TITLE] [--trigger-name NAME] [--replace-trigger N] [--append] [--keep-attachment] [--text|--json]
```

| flag | value |
| --- | --- |
| `--xs` | `file.xs` **(required)** |
| `--title` | `TITLE` |
| `--trigger-name` | `NAME` |
| `--replace-trigger` | `N` |
| `--append` | _(boolean)_ |
| `--keep-attachment` | _(boolean)_ |

**writes output files**

- probe: `skipped_mutating`

### `scen xs extract`

```
kit scen xs extract <file.aoe2scenario> --out file.xs [--carrier N|--trigger N] [--force] [--text|--json]
```

| flag | value |
| --- | --- |
| `--out` | `file.xs` **(required)** |
| `--carrier` | `N|--trigger` |
| `--force` | _(boolean)_ |

**writes output files**

- probe: `skipped_mutating`

## swatch

### `swatch patterns`

```
kit swatch patterns <list|summary|validate|json>
```

Subcommands: `list`, `summary`, `validate`, `json`

- probe: `group_entry`

## xs

### `xs bridge`

```
kit xs bridge <variables.json> [--out-dir DIR]
```

| flag | value |
| --- | --- |
| `--out-dir` | `DIR` |

**writes output files**

- probe: `skipped_mutating`

### `xs datagen`

```
kit xs datagen <arrays.json> [--out module.xs]
```

| flag | value |
| --- | --- |
| `--out` | `module.xs` |

**writes output files**

- probe: `skipped_mutating`

### `xs inspect`

```
kit xs inspect <file.xsdat> [--types string,int,...] [--text]
```

| flag | value |
| --- | --- |
| `--types` | `string,int,...` |
| `--text` | _(boolean)_ |

- probe: `skipped_no_fixture`

### `xs shims`

```
kit xs shims <file-or-dir>... [--out recipe.json] [--prefix TEXT] [--enabled] [--include-params]
```

| flag | value |
| --- | --- |
| `--out` | `recipe.json` |
| `--prefix` | `TEXT` |
| `--enabled` | _(boolean)_ |
| `--include-params` | _(boolean)_ |

**writes output files**

- probe: `skipped_mutating`

## xsdat

### `xsdat decode`

```
kit xsdat decode <file.xsdat> [--types string,int,...] [--ledger expected.json] [--schema rtv12|rtv13|rtv14|rtv141|rtv142|rtv15|rtv16|rtv17|a2ksem2] [--text]
```

| flag | value |
| --- | --- |
| `--types` | `string,int,...` |
| `--ledger` | `expected.json` |
| `--schema` | `rtv12|rtv13|rtv14|rtv141|rtv142|rtv15|rtv16|rtv17|a2ksem2` |
| `--text` | _(boolean)_ |

- probe: `skipped_no_fixture`

